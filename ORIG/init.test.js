//npm run test

describe('setup-test', () => {
  test('1 equals 1', () => {
    expect(1).toBe(1);
  })
  test('2 equals 2', () => {
    expect(2).toBe(2);
  })
  test('3 equals 3', () => {
    expect(3).toBe(3);
  })
})